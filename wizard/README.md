This script will prompt user to enter new superuser password
Will change:
    the root password ( affected file /etc/shadow)
    the tahoe public_node credentials in method2
    the DEF encrypted LUKS password
